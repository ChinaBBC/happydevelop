<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".driver.activity.OrderActivity">

    <include
        android:id="@+id/order_center_title"
        layout="@layout/a_common_title"></include>

    <android.support.design.widget.TabLayout
        android:id="@+id/order_center_tab"
        android:layout_width="0dp"
        android:layout_height="@dimen/dp_40"
        app:tabTextAppearance="@style/TabLayoutStyle"
        app:tabIndicatorColor="@color/color_703f"
        app:tabIndicatorHeight="@dimen/dp_1"
        app:tabSelectedTextColor="@color/color_703f"
        app:tabTextColor="@color/color_6666"
        android:background="@color/white"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/order_center_title"
        ></android.support.design.widget.TabLayout>
    <android.support.v4.view.ViewPager
        android:id="@+id/order_center_pager"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/order_center_tab"
        app:layout_constraintBottom_toBottomOf="parent"
        ></android.support.v4.view.ViewPager>
</android.support.constraint.ConstraintLayout>